# Cloudflare Pages 部署配置
NODE_VERSION=20

# 构建命令
BUILD_COMMAND=npm run build

# 输出目录
OUTPUT_DIR=dist

# 路由重定向（Vue SPA路由）
[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
