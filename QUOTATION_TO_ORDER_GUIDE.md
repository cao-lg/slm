# 报价转订单功能使用说明

## 功能概述

根据 `spec.md` 中的报价单管理业务流程（3.2节），实现了报价单转销售订单功能。

## 业务流程

```
询价接待 → 生成报价单 → 报价接受 → 转为销售订单 → 生产/备货 → 发货出库 → 开具应收 → 收款核销
```

## 操作流程

### 1. 创建报价单
1. 进入 **销售管理 → 报价单**
2. 点击 **新增报价单** 按钮
3. 选择客户
4. 添加产品明细（产品、数量、单价）
5. 系统自动计算金额
6. 保存报价单

### 2. 接受报价
1. 在报价单列表中，找到状态为 **"待确认"** 的报价单
2. 点击 **"接受"** 按钮
3. 报价单状态变为 **"已接受"**

### 3. 转为销售订单（核心功能）
1. 在报价单列表中，找到状态为 **"已接受"** 的报价单
2. 点击 **"转为订单"** 按钮
3. 系统弹出确认框
4. 确认后：
   - 自动创建销售订单
   - 自动更新报价单状态为 **"已转化"**
   - 跳转到销售订单页面

### 4. 确认销售订单
1. 系统自动带入以下数据：
   - ✅ 客户信息（从报价单带入）
   - ✅ 产品明细（从报价单带入）
   - ✅ 单价（从报价单带入）
   - ✅ 金额（自动计算）
2. 手动填写 **交货日期**（必填）
3. 点击 **确定** 保存订单

## 状态流转

### 报价单状态
```
pending（待确认）→ accepted（已接受）→ rejected（已拒绝）
                       ↓
                  converted（已转化）
```

### 销售订单状态
```
pending（待审核）→ approved（已审核）→ producing（生产中）→ shipped（已发货）→ completed（已完成）
```

## Mock数据说明

系统已配置完整的Mock数据：

- 3个报价单（1个已接受，1个待确认，1个已拒绝）
- 每个报价单包含产品明细
- 客户和产品信息完整

## 代码变更

### 1. API接口更新
**文件**: `/workspace/erp-frontend/src/api/sales.ts`
- 添加 `convertQuotationToOrder()` 接口

### 2. 报价单列表页面
**文件**: `/workspace/erp-frontend/src/views/sales/QuotationList.vue`
- 添加 **"转为订单"** 按钮（只在状态为"已接受"时显示）
- 更新状态文本（pending → 待确认，accepted → 已接受）
- 添加 `handleConvertToOrder()` 函数

### 3. 销售订单创建页面
**文件**: `/workspace/erp-frontend/src/views/sales/SalesOrderList.vue`
- 添加从报价单带入数据的功能
- 支持URL参数自动带入报价单数据
- 添加 `handleConvertFromQuotation()` 函数

### 4. Mock数据
**文件**: `/workspace/erp-frontend/src/utils/request.ts`
- 更新报价单状态为 "accepted"
- 添加报价单详情Mock数据
- 添加报价单明细Mock数据
- 添加报价转订单Mock数据

## 验收标准

- [x] 报价单列表显示"转为订单"按钮
- [x] 按钮只在状态为"accepted"时显示
- [x] 点击后跳转到销售订单创建页面
- [x] 客户信息正确带入
- [x] 产品明细正确带入
- [x] 单价正确带入
- [x] 报价单状态变为"accepted"

## 注意事项

1. 只有状态为 **"已接受"** 的报价单才能转为订单
2. 转为订单后，报价单状态自动更新为 **"已转化"**
3. 交货日期需要手动填写（必填）
4. 所有数据变更通过Mock API模拟，实际使用时需要后端支持

## 后续优化建议

1. 添加报价单版本历史功能
2. 支持修改已接受的报价单（需重新审核）
3. 添加报价有效期提醒
4. 支持批量转为订单
5. 添加转换记录查询功能
